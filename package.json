{
  "name": "peptriva-review-network",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Disclosed affiliated publications examining research supplier documentation.",
  "scripts": {
    "build": "node build.mjs --all",
    "build:review": "node build.mjs --site peptriva-review",
    "build:reviews": "node build.mjs --site peptriva-reviews",
    "build:customer": "node build.mjs --site peptriva-customer-reviews",
    "build:ratings": "node build.mjs --site peptriva-ratings",
    "check": "node scripts/compliance-check.mjs",
    "serve": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
